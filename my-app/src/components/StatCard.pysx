def StatCard(title, value, trend):
    return <div class="stat-card">
        <h3 class="stat-title">{title}</h3>
        <div class="stat-value">{value}</div>
        <div class="stat-trend">{trend}</div>
    </div>
